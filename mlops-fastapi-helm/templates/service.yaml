apiVersion: v1
kind: Service
metadata:
  name: {{ include "mlops-fastapi.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "mlops-fastapi.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
